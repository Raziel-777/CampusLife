import {
    StyleSheet,
} from 'react-native';

export default styles = StyleSheet.create({
    title: {
        fontSize: 30,
        color: 'black',
        fontWeight: 'bold',
        fontFamily: 'Tahoma',
    }
})